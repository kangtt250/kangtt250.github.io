# Seon Zhu Personal Website

Personal website and blog built with Jekyll and a customized Hydejack-derived layout.

## Local Development

```sh
bundle install
bundle exec jekyll serve
```

The generated site is written to `_site/`, which is intentionally ignored by Git.

## Deploy

Push changes to the GitHub Pages branch and let GitHub build the site.
